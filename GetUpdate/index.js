/*

    A function for downloading an update for a particular node/system.

*/

module.exports = function (context, req) {
    res = {
        body: "This function is not yet implemented"
    };

    context.done(null, res);
};